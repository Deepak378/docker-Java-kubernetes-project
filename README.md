# docker-Java-kubernetes-project
steps:
  install maven
  clone the repo
  build all three java projects using maven and verify the jar in target
  do docker build for dockerfile in all respective microservices appln.
  docker tag and docker push
  update the image name in kubernetes folder in all yaml
